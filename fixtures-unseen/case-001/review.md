# Review

## Weaknesses

1. The paper does not report the planner-only baseline, so the scheduler gain is unsupported.
2. The paper never runs model-tier cost ablations, so the chosen workflow may be uneconomical.
3. The system should ship a mobile notification app for annotators.
